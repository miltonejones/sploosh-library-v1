import React from "react";
import TagWordInfoCard from "./TagWordInfoCard";

export default {
  title: "Modal/TagWordInfoCard",
  component: TagWordInfoCard,
};

const Template = (args) => <TagWordInfoCard {...args} />;

export const DefaultView = Template.bind({});

DefaultView.args = { tag: "shemale fucks guy" };
