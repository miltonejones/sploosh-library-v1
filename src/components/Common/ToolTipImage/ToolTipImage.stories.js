import React from "react";
import ToolTipImage from "./ToolTipImage";

export default {
  title: "Common/ToolTipImage",
  component: ToolTipImage,
};

const Template = (args) => <ToolTipImage {...args} />;

export const DefaultView = Template.bind({});

DefaultView.args = {};
